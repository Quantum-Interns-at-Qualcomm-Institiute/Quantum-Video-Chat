# Runtime dependencies for the signaling server. Keep pins at or above the
# versions Dependabot's advisories name as first-patched.
Flask==3.1.3
Flask-CORS==6.0.1
python-socketio==5.16.2
eventlet==0.40.3
