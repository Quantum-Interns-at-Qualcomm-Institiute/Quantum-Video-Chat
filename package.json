{
  "name": "quantum-video-chat",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "concurrently -n SERVER,CLIENT -c cyan.bold,yellow.bold --kill-others-on-fail \"npm run start:server\" \"cross-env QUANTUM_SERVER_HOST=127.0.0.1 QUANTUM_SERVER_PORT=5050 npm run start:client\"",
    "start:client": "bash start.sh client",
    "start:server": "bash start.sh server"
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "cross-env": "^7.0.3"
  }
}
